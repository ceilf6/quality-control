# 质量控制中最重要的概率分布

## 1. 二项分布与正态分布

| 项目 | 二项分布 | 正态分布（或高斯分布） |
|---|---|---|
| 概率密度 / 概率函数 | $P(X = k) = C_n^k p^k(1-p)^{n-k} = \dfrac{n!}{k!(n-k)!}p^k(1-p)^{n-k}$ | $f(x)=\dfrac{1}{\sigma\sqrt{2\pi}}e^{-\frac{(x-m)^2}{2\sigma^2}}$ |
| 平均值 | $E(X)=n\cdot p$ | $E(X)=m$ |
| 方差 | $V(X)=np(1-p)$ | $V(X)=\sigma^2$ |

## 2. 标准正态分布

任何正态分布 $N(m,\sigma)$ 都可以通过变量变换转化为标准正态分布：

$$
u = \frac{x-m}{\sigma}$$

其中：

- $x$：原始变量值
- $m$：均值
- $\sigma$：标准差
- $u$：标准化后的变量，服从标准正态分布
